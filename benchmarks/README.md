# Benchmarks

This directory is reserved for ESLint performance benchmarks once concrete
runtime-cleanup rules exist.

The initial scaffold intentionally does not carry over benchmark scenarios for
template-only rules.
