/**
 * Placeholder benchmark stats command for the initial scaffold.
 */

export const benchmarkStatsMessage =
    "No runtime-cleanup benchmark scenarios are defined yet.";

console.log(benchmarkStatsMessage);
